version: '3.8'

services:
  mongo:
    image: mongo:6
    container_name: mongo_lab
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  web:
    build: ./app
    container_name: flask_app
    ports:
      - "5000:5000"
    depends_on:
      - mongo

volumes:
  mongo_data: